{
  "name": "components",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "dev": "pnpm -r --filter \"./packages/*\" dev --parallel",
    "typecheck": "pnpm -r --filter \"./packages/*\" typecheck",
    "preview": "python3 -m http.server 8317",
    "cdn": "node scripts/cdn.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
