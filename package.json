{
  "name": "components",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "dev": "pnpm -r --filter \"./packages/*\" dev --parallel",
    "typecheck": "pnpm -r --filter \"./packages/*\" typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
